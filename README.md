# AI Storytelling Studio

A premium cinematic portfolio site for an AI video storytelling studio, built with Flask, HTML, CSS, and JavaScript.

## Run locally

1. Install Python 3.10+.
2. In this folder, install dependencies:
   `pip install -r requirements.txt`
3. Start the app:
   `python app.py`
4. Open the local URL shown in the terminal.
# chitrakala-portfolio
